/**
 * Encore Design System
 *
 * Aesthetic: Refined Dark Studio
 * Fonts: Bricolage Grotesque (display) + Geist Sans (body) + Geist Mono (code)
 * Accent: Amber/Gold (#f5a623) — "the spotlight"
 *
 * Usage:
 *   - CSS: Use Tailwind utilities with custom theme values, or var(--token-name) directly
 *   - TS: Import { colors, fonts, ... } from '@renderer/design-system' for programmatic use
 */

export { colors, fonts, fontSizes, radii, shadows, transitions } from './tokens'
